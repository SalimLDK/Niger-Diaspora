---
name: project_feed_discussion_redesign
description: Refonte design Fil & Discussion + tours 7-28 — quasi tout l'ordre d'implémentation fait sur branche wip-jules (23 commits) ; reste = données/modèles absents + WIP user
metadata: 
  node_type: memory
  type: project
  originSessionId: b201b14c-940d-4e6c-b370-eef29b2bdbff
  modified: 2026-07-28T19:27:22.352Z
---

Refonte UI du fil (`feed`) et de la discussion (`messages`) d'après le handoff
`Amélioration feeds et discussion/design_handoff_feed_discussion/README.md`
(prototype HTML `Feed & Discussion.dc.html` = référence qui fait foi ; tour 4 =
direction retenue, tour 5 = écrans satellites, tour 6 = Nocturne sombre).

**Fait (2026-07-28, étapes 1-3 de l'ordre d'implémentation) :**
1. `FeedTokens` : ajout `textStrong` (= text), `actionLabel`, `actionMuted`, `hairline` (clair + sombre).
2. `post_card.dart` : nom auteur 15/w600, sous-ligne 12.5 mutedText, corps 15.5/height 1.6/textStrong, média rayon 20(clair)/8(sombre) h≈205, barre d'actions avec filet `hairline` + couleurs actionLabel (gauche : cœur/commentaire/repartage) / actionMuted (droite : signet/partage) + accent quand actif, icônes 19px. « Suivre » = variant texte accent (`FollowButtonVariant.text` ajouté à `follow_button.dart`, défaut `filled` inchangé pour follows_screen/reposters_screen).
3. `message_bubble.dart` : bulles opaques (retrait `BackdropFilter`+dégradés+bordure blanche, import `dart:ui` retiré) via `_bubbleDecoration()` — envoyée `#1B5E32`/`#2D7D46`, reçue `#FFFFFF`+bord `#EFE7DB` / `#252119`+bord `#3D352C` ; rayons 18 coin queue 6 ; horodatage unique sous le dernier du groupe (`_isLastInGroup`) ; **fix contraste** : time/status reçu passe de blanc 0.7 (invisible sur bulle claire) à `textTertiaryColor` (`metaColor`) ; glisser-répondre borné 90px, seuil 52px.

**Fait (2026-07-28, étapes 4-10) :**
4. `message_input.dart` : « + » hors du champ → panneau ancré grille 3×2 (`_buildAttachPanel` : Caméra/Galerie/Document/Position/Sondage/Événement ; ancien `showModalBottomSheet` conservé en repli sur appui long du « + » pour garder audio/vidéo dédiés) ; champ pilule avec emoji **à l'intérieur** ; composer opaque (retrait `BackdropFilter` blur 56 + import `dart:ui`/`AppShadows`) fond #FFFFFF/#1A1714 ; bouton vocal = pilule verte « MAINTENIR » (`_isVoicePill`, `_kVoiceGreen`) → rond d'envoi dès qu'il y a du texte ; `_buildRecordingBanner` colore le bandeau des 3 états. **Gestes d'enregistrement inchangés** (le `_buildPersistentActionButton` reste un seul GestureDetector).
5. `audio_message_bubble.dart` : `_WaveformPainter` (18 barres 4px gap 3.5, ~132×36), tête de lecture filet 2px + pastille 8px cerclée bulle, seek au clic, pilule vitesse en ligne, `pos / total` + point vert « non écouté » (`_hasBeenPlayed`). Play 44px vert(reçu)/blanc(envoyé).
6. `feed_screen.dart` : `_FeedHeader` (date `MaterialLocalizations.formatFullDate` maj + « Le fil. » point accent + recherche→`/search` + avatar `_MySpaceAvatar`→`/feed/space`), segments pleine largeur (`FeedSegmentedControl.fullWidth` ajouté), réserve basse 100px. **Filtres villes + rail d'actus non faits** (pas de source de données : ville auteur + stories).
7. `conversation_screen.dart` : sous-barre `_buildQuickSubBar` (tuiles `_SubBarTile`) = Médias→`/messages/:id/media` + bascule ÉCO liée à `PreferencesService.dataSaverMode` (feature réelle). Pastille épinglé = déjà couverte par `GroupPinnedBanner`. En-tête 58/38px non retouché (app bar existante conservée).
8. Nouveau `mon_espace_screen.dart` route `/feed/space` (déclarée AVANT `/feed/:postId`) : avatar 64, nom/@handle, 2 cartes stats (`followersCountProvider`/`followingCountProvider`), tuiles → `/profile/my-posts`, `/profile/reposts`, `/profile/saved-posts`, `/profile/follows`. Écrans tour 5 existants réutilisés tels quels (restyle segments/chips/états-vides non fait).
9. Tablette : colonne centrale bornée `maxWidth 640` centrée (largeur ≥700). Rail nav 86px + colonne droite 330px **non faits** (dupliquerait le shell + panneaux sans données).
10. Nocturne : géré par construction (tokens + `context.isDarkMode` partout) ; FAB contour indigo déjà via `fabBorder` nocturne.

**Fait (2026-07-28, 2ᵉ session — dossier `Amélioration feeds et discussion2/` = re-livraison superset : ajoute tours 7-28 + `GUIDE_IMPLEMENTATION.md` ; le spec des étapes 1-3 est identique et DÉJÀ commité, commits `eea1793`/`638e2d6`) :**
- Polish : bouton **download** dans `audio_message_bubble.dart` (notes reçues, à droite des contrôles, via `FileDownloadService.downloadToAppDirectory` + `getDownloadedPath`, coche verte `download_done`). RÉCENTS emoji = **déjà fourni par le package** `emoji_picker_flutter` (`RecentTabBehavior.RECENT` par défaut, 1er onglet catégorie) → pas de rangée custom (fighterait la nav du package).
- Restyle tour 5 : nouveau widget réutilisable `feed_empty_state.dart` (cercle 104 + icône accent, titre 21 Caprasimo, corps, amorces cliquables, CTA plein, note). Appliqué à `my_posts`, `reposts`, `saved_posts`, `follows`. `saved_posts` = chips Tout/Photos/Vidéos/Textes (filtre client par `PostMediaType`) + regroupement Cette semaine/Plus ancien (`SliverMainAxisGroup`). `follows` = `FeedSegmentedControl.fullWidth` avec compteurs (remplace `TabBar` souligné) ; recherche + suggestions **omises** (liste = IDs, noms async → pas de filtre fiable, pas de provider suggestions).
- Feed (`feed_screen.dart`) : **filtre PAYS** (pas ville — `PostEntity` n'a que `authorCountry`, pas de `authorCity`) en chips scrollables sur téléphone + panneau dans le rail droit tablette ; filtre **client sur posts chargés** (pas de filtre serveur). **Rail d'actus/stories NON fait** (aucun modèle). Layout tablette (≥700) = colonne centrale bornée 640 + **rail droit 330px** (`_FeedRightRail` : filtres pays + hashtags du moment dérivés de `PostEntity.hashtags`, tap → `/feed?hashtag=`) ; **pas de rail nav gauche** (géré par le shell, éviterait la double nav) ; FAB 64px sur tablette.
- l10n : ~20 clés ajoutées dans `app_fr.arb`+`app_en.arb` (titres/corps états vides, videos/texts, older, trendingHashtags, noPostsForFilter…). `flutter analyze` = 0 issue sur tous les fichiers touchés. Pas de vérif device.

**Fait (2026-07-28, 3ᵉ session — suite de l'ordre d'implémentation, commités+poussés sur `wip-jules-2025-12-29T23-58-34-776Z`) :**
- **Étape 11 Accueil** (`home_screen.dart`, commit `6245f94`) : dégradé orange retiré → en-tête plat, tuiles QR/notif 44px, ligne de contexte « ville · N membres · M groupes » (remplace les 3 cartes stats), bloc « Aujourd'hui » (unread `totalUnreadCountProvider` + prochain event + membres proches), services en grille 4 colonnes. Coach marks/timers/flags préservés.
- **Étape 12 Carte** (`map_screen.dart`, commit `6a69f3f`, PARTIELLE) : légende → bouton « ? » (feuille modale, retire le calcul `bottom: 130/290` conditionnel). **Reste (non fait, réécritures d'un écran Google Maps 3200 lignes NON testable sans device) : panneau membres en `DraggableScrollableSheet` 3 positions, en-tête unifié search+chips, écran 8c sans localisation dédié.**
- **Étape 13 Groupes** (`groups_screen.dart`, commit `fb0c02c`, PARTIELLE) : cadenas si privé + chip ville sur `_GroupCard`. **Badge ACTIF/CALME bloqué : `GroupEntity` n'expose pas `lastMessageAt` (présent au backend `group_remote_datasource.dart:318`, non mappé) → nécessiterait ajout entity+model+mapper.**

Mur récurrent : les features visuelles à fort impact dépendent de données/modèles absents (stories, ville post, activité groupe, épinglés) ou d'écrans complexes non testables sans émulateur. `messages_screen` déjà refait en `638e2d6`.

**Fait (2026-07-28, 4ᵉ session — étapes 14-20 en passes partielles sûres, commités+poussés) :**
- **14 Profil** (`bb8ca03`) : en-tête plat, e-mail retiré, avatar 84, badge vérifié, ville/pays.
- **15 Transfert** (`4834e44`) : bouton porte le total débité.
- **16 Boutique** (`546998a`) : prix en premier sur `product_card`.
- **17 Événements** (`dede863`) : badge « Complet » (attendeeIds>=maxAttendees).
- **Déjà alignés (aucun changement) :** historique d'appels §12 (`_CallHistoryItem` : manqués en rouge, icône directionnelle, type·heure·durée), onboarding §15 (skip top-right, puce active 32×8, CTA pleine largeur), `send_tip_bottom_sheet` §17 (paliers 1/2/5/10/20/50, « 95% », handle 40, bouton « Envoyer €x ») — la feature audio_rooms a été bâtie sur la maquette.
- Ces « réécritures larges » ont **toutes été faites en 5ᵉ session** (ci-dessous).

**Fait (2026-07-28, 5ᵉ session — les réécritures larges + modales + reconcile, tout commité+poussé sur `wip-jules-2025-12-29T23-58-34-776Z`) :**
- **Notifications 6→3** (`b97ce16`) : enum `{all,unread,mentions}`, TabBar 6 onglets → 3 puces (Tout / Non lues+badge / Mentions), TabController retiré. « Mentions » = pas de `NotificationType` dédié → regroupe types perso (friendRequest, newFollower, groupInvite, eventAttendance, nearbyMember…).
- **Recherche compteurs** (`a3bf6b0`) : `_resultCountFor` sur listes brutes de `SearchState` (indépendant du filtre actif, masqué sans requête).
- **Groupes badge ACTIF/CALME** (`13e4536`) : PAS un champ mort sur `GroupEntity` — `lastMessageAt` vit sur la conversation liée (`conversations.groupId`), pas le doc groupe. Nouveau `groupLastActivityProvider` (Map<groupId,DateTime>) dérivé de `conversationsProvider` DÉJÀ chargé → live, zéro requête. `_GroupCard`→ConsumerWidget. ACTIF si <24h.
- **Transfert montant héros + stepper** (`b1230c4`) : Stepper natif → indicateur 1 ligne (rond 22px + barres accent/à-venir #B85E24/#E8DFD4) + contenu scroll + barre boutons fixe (principal porte le total). Montant 40px w700 ls -1.5 + devise en pastille (PopupMenuButton sur `_transferCurrencies`) + 4 montants rapides. Retrait `_amountValid`/`_getCurrencySymbol`/`_buildTransferCurrencyItems`.
- **Profil réglages 7→3** (`4fa4a1c`) : 5 sections → 3 ExpansionTiles repliables (`_buildSettingsGroup`) avec état en sous-titre. **Choix : grouper EN PLACE, pas naviguer vers `SettingsScreen`** (hub orphelin, route `/settings` commentée) car il ne réplique pas tout (noiseSuppression/chatBackground/keyBackup) → sinon perte d'accès. Mon espace + zone danger restent séparés.
- **Carte panneau membres** (`a144bbf`) : `Positioned+AnimatedSwitcher` → `DraggableScrollableSheet` snap 18/45/92%, liste verticale `_buildMemberSheetItem` (avatar 44, métier·ville, bouton 40px). Retrait masquer/rouvrir (`_isMembersPanelHidden`, `_buildMembersReopenChip`). Bloc ~270 lignes remplacé par script python paren-matching (trop grand pour Edit exact).
- **Carte 8c sans localisation** (`6bfdcdd`) : body → `_buildNoLocationScreen` quand `_isReciprocityRestricted` (cercle 104 + réciprocité + 3 garanties + CTA ACTIVER + repli Ambassades/Groupes). Ancien bandeau chips = branche morte laissée.
- **Ambassade bandeau** (`95e5fe9`) : bandeau unifié vert « Ouvert »+horaires du jour / rouge « Temporairement fermé »+réouverture. PAS de « ouvert maintenant » (format `openingHours` back non garanti) → `_todayHours()` best-effort clé jour FR/EN.
- **Modales §16** : suppression msg `ee2dcaa` (délai rappelé + destructif isolé sous filet), signalement `36a3841` (bascule « bloquer aussi cet auteur » via `blockUserNotifierProvider` + anonymat/48h annoncés), CTA média `bec5867` (`media_preview_screen` FAB→FAB.extended « Envoyer la photo/vidéo/document »). `_showAttachmentOptions` déjà §16-conforme. Passe complète ~43 feuilles = tail incrémental non fait.
- **Reconcile travaux d'autres sessions dans l'arbre** : `e33c6eb` épinglage photo/vidéo (onLongPress→menu complet sur OptimizedImageBubble/VideoBubble + câblage message_bubble + `_refreshPinnedBanner` conversation_screen + ciphertext-safety group_pinned_banner) ; `ecddbf1` chore dart format datasource. **Méthode d'audit avant commit : `git diff -w` pour séparer reformat de logique.**

**Fait (2026-07-28, 6ᵉ session — bundle `Amélioration feeds et discussion2/` = superset + `GUIDE_IMPLEMENTATION.md` ; le GUIDE donne le « comment » écran par écran, valeurs exactes. Cluster Fil §13 choisi par l'utilisateur, commité+poussé) :**
- **poll_results §13** (`2288796`) : barres remplies au prorata (`Stack`/`FractionallySizedBox` : fond `tokens.surface` + remplissage `tokens.accent2` @35%) au lieu de `LinearProgressIndicator` ; option gagnante (voteCount==max & totalVotes>0) encadrée 1,5px `accent2` via `context.cardDecoration.copyWith(border:)` ; pastille « Votre choix » sur `poll.votedOptionIds.contains(option.id)` ; rappel visibilité en pied. Import `FeedTokens` ajouté au screen (utilisait AdaptiveColors + `_pollAccent` violet, conservés pour le chrome).
- **post_detail §13** (`54ec526`) : `_ActionBar` de `post_card` (partagé, flag `isDetail`) → compteurs nommés en détail seulement (`${likeCount} ${l10n.likes}` = « 24 J'aime » ; `l10n.postComments(commentCount)`), liste reste compacte. `comment_tile` : réponses indent 44→42, avatar reply r13→r15 (30px), action like nommée « J'aime · N » (accent si aimé) à côté de « Répondre ». Composer `_CommentInput` : `FeedAvatar` de l'utilisateur courant (FirebaseAuth) ajouté à gauche.
- **create_post §13** (`e3ff928`) : puce audience « Public » (icône+chevron) cliquable → `_showAudienceSheet` (posts toujours publics, pas d'audience au modèle) ; compteur média « N/5 » sous la saisie ; rappel « visibles par toute la diaspora » en pied. Import `feed_tag` retiré (FeedTag remplacé). **Coloration live des hashtags dans le champ NON faite** (`MentionTextField` partagé avec commentaires → risque overlay mentions sans device).
- l10n : aucune clé ajoutée (réutilise `likes`, `postComments`, `reply`). `flutter analyze` = 0 issue sur chaque fichier. Pas de vérif device.
- **Branche partagée live** : l'agent Jules a commité en parallèle pendant la session le WIP composer (`96d70fd` auteur Salim, composer flottant) + le bundle 2 (`8be8657`) → working tree redevenu clean, mes 3 commits en fast-forward par-dessus. **Rien de perdu** ; le WIP n'était plus à préserver (déjà commité).

**Fait (2026-07-28, 7ᵉ session — Discussion §14, 3 commits poussés) :**
- **Favoris §14** (`e909427`, `starred_messages_screen`) : notes vocales/audio en bloc dédié (mic + « Note vocale · 0:34 » via `audioDurationFormatted`) au lieu de texte plat ; affordance explicite « Aller au message » (chevron accent) sous chaque extrait. Écran per-conversation → « origine » = auteur (déjà là).
- **Galerie §14** (`94b5f31`) : **onglet Vidéos ajouté** (3 onglets « Photos·N / Vidéos·N / Documents·N », TabBar scrollable). Les 2 datasources `getMediaMessages` filtraient `['image','file']` → ajout `'video'` (Supabase `inFilter` L2051 + RTDB filtre L2293). `MediaGalleryState` gagne `videos` (état écrit main, pas de build_runner). `_VideosTab`/`_VideoGridItem` (vignette `thumbnailUrl` + voile + play + pastille durée depuis `videoDuration`, tap = launchUrl externe). Bandeau « conservés 15 jours sur le serveur » sous les onglets.
- **Nouvelle conversation §14** (`563b804`) : état au repos (sans recherche) = raccourcis **Nouveau groupe** (focus recherche + hint) / **Mes notes** (`ensureSelfNotesProvider.notifier.ensure()` + push `isSelfNotes:true`) + **Contacts récents** (convs `isIndividual && !isSelfNotesFor` de `conversationsProvider`, tap → push avec `getOtherParticipantId`). Ancien placeholder → « Aucun résultat » (recherche vide). **« Proches de vous » (distance/présence) NON câblé** — aucun provider de membres proches dans la feature messages.
- `flutter analyze` = 0 issue par fichier. Pas de vérif device.

**Fait (2026-07-28, 8ᵉ session — Appels §12, 2 commits poussés) :**
- **call_history §12** : DÉJÀ aligné (filtres, icônes `call_missed/received/made`, type·heure·durée) → aucun changement.
- **call_screen §12** (`3de4235`) : `_buildModernCallControls` — barre WhatsApp (rangée + raccrocher centre) → **4 contrôles nommés 64px** (`_buildNamedControl` icône+libellé) + **« Raccrocher » pleine largeur 68px** (`_buildFullWidthHangup`). Handlers WebRTC inchangés ; anciens `_buildWhatsAppButton/HangupButton` supprimés (lint unused_element). **10 clés l10n** (`callControl*`, `hangUp`) insérées après `@@locale` (clés simples sans @metadata) + `flutter gen-l10n` (l10n généré tracké → commité).
- **group_call_screen §12** (`d78689c`) : `childAspectRatio` 1→0.82 ; orateur actif → `Color(0xFF5BA674)` 2px ; **badge « VIDÉO »** (#5BA674) si `isVideo && !isCameraOff` (micro coupé rouge déjà là).
- ⚠️ `flutter analyze` très lent (~108s/fichier cold cache) → timeout ≥300000ms. 0 issue.

**Fait (2026-07-28, 9ᵉ session — Support §11, 3 commits poussés) :**
- **ticket_detail §11** (`01416e6`) : ticket = conversation DÉJÀ en place (support gauche `support_agent`, user droite, composer). Ajouts : **carte de contexte** en tête (`_buildContextCard` : catégorie, chip état, transfert lié si `relatedTransactionId`, date) + **bulles envoyées vertes** (`AppColors.secondaryDark`/`secondary`, texte blanc).
- **support_tickets état vide §11** (`a9e5334`) : `_EmptyStatePrompts` = 3 amorces typées (transfert→transaction / compte→account / bug→technical) pré-remplissant sujet+catégorie → push `/support/new` extra `{subject, category}`. `CreateTicketScreen` gagne `initialCategory` (route via `extra['category'] as TicketCategory?`). 4 clés l10n.
- **create_ticket §11** (`42283b5`) : bloc **« Joint automatiquement — Diaspo Niger · <OS> · <langue> »** (sync `dart:io Platform` + locale). Version app + modèle NON affichés (pas de `package_info_plus` ; `device_info_plus ^11.4.0` dispo). 1 clé l10n. « Rattacher transfert » déjà supporté (`relatedTransactionId`), non étendu côté écran transfert.

**Fait (2026-07-28, 10ᵉ session — passe modales §16, début, 2 commits poussés) :**
- **Contexte** : 45 `showModalBottomSheet` dans lib. Beaucoup ONT déjà une poignée (audio_rooms/feed/groups/events/businesses/calls/create_poll). Headline §27b/c/d (suppression/signalement/média) déjà faits en 5ᵉ session. Vrais manques = poignées ad hoc incohérentes (couleur `ctx.borderColor`/`dividerColor`, rayon 2 au lieu de #D8CCB8 rayon pilule).
- **Widget partagé `SheetHandle`** (`4390953`, `lib/shared/widgets/sheet_handle.dart`) : barre 40×4 rayon 999, `#D8CCB8` clair / blanc 22% sombre. **Primitive réutilisable** pour toute la passe (Jules inclus).
- **Adopté** dans : `settings_screen` 4 feuilles (`4390953`), `create_poll_sheet` + `profile_screen` 3 feuilles (`649efb8`). 8 poignées standardisées. analyze 0 issue.
- **Suite (tail, 3 commits)** : `conversation_options_modal` (`a7eade0`) = handle + **destructif isolé** (« Supprimer la conversation » déplacé du milieu vers le bas après filet, règle 4). Handle adopté sur 7 feuilles messages de plus : `mute_duration`/`auto_delete_settings`/`message_info`/`note_poll_draft` (`207f81d`), `delete_message_modal`/`location_picker_modal`/`forward_conversation_picker` (`86f4be4`). Espacement préservé via `Padding` quand l'ancienne poignée avait un margin. ~16 feuilles sur SheetHandle au total.
- **Passe poignées TERMINÉE pour les feuilles à palette générique** (3 commits de plus : `a0ce93d`, `dd0115f`, `be20168`). SheetHandle adopté aussi dans : share_to_conversation, blocked_users_modal, payment_accounts, sticker_packs, image_picker_dialog (×2), marketplace, edit_profile, profile_config, messages_screen (Mes notes), map_screen (×2), groups_screen, groups_map_screen, add_participant_modal, review_form_modal. **~28 feuilles au total sur SheetHandle.** Vérif finale (regex `width:40\n height:4 … circular(2)` hors audio_rooms/podcasts/feed) = **0 poignée générique restante**.
- **NE PAS convertir** : audio_rooms/podcasts (palette DNColors) et feed (FeedTokens) gardent leurs poignées propres — SheetHandle est #D8CCB8, hors-palette pour elles.
- **Règles de CONTENU §16 — cluster messages fait** (`f35e89e`, `2f09cb2`) : menu long-press message §27a (`message_bubble` `_showOptionsModal`) = filet (Divider hairline) inséré avant le cluster destructif Supprimer/Signaler (déjà en bas mais sans filet) + poignée→SheetHandle ; menus média repli (`optimized_image_bubble`, `video_bubble`) = filet avant « Supprimer » + poignée ; bouton « Envoyer » de `_ReportDialog` → « Envoyer le signalement ». `delete_message_modal` déjà §27b-conforme (2 portées nommées + conséquences, destructif isolé, délai) → non touché.
- **Constat** : l'app est déjà largement §16-conforme sur les feuilles clés (peu de boutons « OK »/« Confirmer » génériques ; destructifs souvent déjà en bas). Les gains contenu restants sont rares et dispersés (ex. `auto_delete_settings` bouton « Enregistrer » = limite, laissé). Reste éventuel : audio_rooms/podcasts (ghost_moderator destructif, palette DNColors) non audités pour le contenu.

**Fait (2026-07-28, coloration hashtags composer §13, `dde4218`) :** nouveau `HashtagHighlightingController extends TextEditingController` (`feed/presentation/widgets/hashtag_highlighting_controller.dart`) override `buildTextSpan` → colore `#hashtags`+`@mentions` en direct (couleur = `FeedTokens.hashtagColor` réglée à chaque build via champ `highlightColor`, w600). Adopté dans `create_post` et `post_detail` (composer commentaire). **Mon ancienne crainte (MentionTextField partagé) était infondée** : le widget passe le controller tel quel au TextField et ne lit que `.text`/`.selection` → un sous-type de controller est transparent. Repli au rendu standard pendant composition IME. Pas de `notifyListeners` nécessaire (le champ repeint après le build de l'hôte).

**Fait (2026-07-28, « Proches de vous » §14, `68dfa91`) :** section dans l'état au repos de `new_conversation_screen` (avant contacts récents). Source = `nearbyProfilesNotifierProvider` (défini dans `profile/presentation/providers/profile_provider.dart`, `AsyncValue<List<ProfileEntity>>`, partagé avec accueil/carte) + `nearbyMembersEnabledProvider` (gate privé). `ProfileEntity` a `latitude/longitude` (pas de `distanceKm`) → distance calculée via `Geolocator.distanceBetween(_myLat/_myLng, p.lat/lng)`. **Localisation chargée seulement si permission DÉJÀ accordée** (`Geolocator.checkPermission`, pas de prompt depuis cet écran) → si refusée/absente, section masquée. Tap = `createConversationProvider.notifier.createIndividual(p.id)` + push. Présence = `isOnline && showOnlineStatus`.

**Fait (2026-07-28, amorce transfert→support §11, `aae800c`) :** `transaction_detail_screen._contactSupport` avait déjà un bouton support mais en **mailto** + placeholder mort « Chat en direct ». Remplacé le placeholder par « Ouvrir un ticket » (1er, primaire) → `context.push('/support/new', extra: {transactionId, subject, description})`. `_supportPrefill` remplit la description (bénéficiaire/montant/statut). Boucle complète : route → `CreateTicketScreen(relatedTransactionId)` → catégorie Transaction auto + carte de contexte côté `ticket_detail` (faite en 9ᵉ session). Email/téléphone conservés en secondaire.

**Fait (2026-07-28, résidu audio §16/17, `b4c9dfc`) :** nouveau **`DnSheetHandle`** (`lib/shared/widgets/dn_sheet_handle.dart`, barre 40×4 rayon pilule, `context.dn.onSurface4` via `DNThemeExt`) = équivalent DNColors de `SheetHandle` (on ne mélange PAS les deux palettes : `SheetHandle`=#D8CCB8 pour le fil, `DnSheetHandle`=DN pour salons/podcasts). Adopté là où la poignée était incohérente (`Colors.grey[300]`/`context.borderColor` rayon 2) : heritage_library, collection_progress, content_pickers (×2), episode_tile (×2), save_as_podcast_sheet. `buy_ticket`/`send_tip` utilisaient déjà `dn.onSurface4` → laissés (couleur identique). **`ghost_moderator_screen` = un ÉCRAN, pas une feuille** → règles §16-modales inapplicables ; son contenu §17 (bandeau invisibilité, « muet en silence ne prévient pas ») = concern feature séparé, hors passe modales.

**Reste — Handoff README + GUIDE_IMPLEMENTATION essentiellement TERMINÉ.** Ne restent QUE des items bloqués par données/modèles/décision produit, jamais faisables sans backend :
- **rail stories/actus** (aucun modèle Story) ; **filtre par ville** du fil (`PostEntity` n'a que `authorCountry`, pas `authorCity`) ; **filtre serveur** (fait en client) ; **en-tête carte unifié search+chips** (réécriture d'un écran Google Maps 3200 lignes non testable sans device) ; **back-office admin** (19 écrans, doc séparée).
- `message_input.dart` = composer flottant commité par Jules (96d70fd).
- **Aucune vérif device** (pas d'émulateur) — les gros changements de gestes/layout (map sheet, transfert stepper, contrôles d'appel, « Proches de vous », coloration hashtags) restent à valider à la main.
Voir [[project_feed_roadmap.md]], [[project_message_dedup_typing.md]], [[project_supabase_over_firebase.md]].
