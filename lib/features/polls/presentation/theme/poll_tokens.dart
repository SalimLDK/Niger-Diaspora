import 'package:flutter/material.dart';

/// Violet du sondage — une seule definition.
///
/// La meme valeur etait recopiee a l'identique dans cinq fichiers (la carte,
/// la feuille de creation, l'ecran de resultats, le menu « + » de la
/// conversation, le brouillon de « Mes notes ») : cinq occasions de deriver
/// sans que rien ne le signale.
const Color kPollAccent = Color(0xFF6B5CE0);
