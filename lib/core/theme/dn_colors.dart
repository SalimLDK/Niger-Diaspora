import 'package:flutter/material.dart';

/// Sahel design palette — Audio Rooms feature tokens.
/// Usage: `DNColors.terra` etc.
class DNColors {
  DNColors._();

  // Ink scale (dark → light)
  static const ink = Color(0xFF1A1410);
  static const ink2 = Color(0xFF3D342B);
  static const ink3 = Color(0xFF7A6F64);
  static const ink4 = Color(0xFFB8AC9D);

  // Paper scale (warm white)
  static const paper = Color(0xFFFAF6EF);
  static const paper2 = Color(0xFFF0E8D8);
  static const sand = Color(0xFFE8DCC4);

  // Primary — terracotta
  static const terra = Color(0xFFC85A3A);
  static const terra2 = Color(0xFF9A3A20);

  // Accents
  static const ochre = Color(0xFFD9A441);
  static const teal = Color(0xFF2D6E6A);
  static const leaf = Color(0xFF5A7A3A);
  static const mark = Color(0xFFF6EF5A);
  static const danger = Color(0xFFC23E2D);
}
